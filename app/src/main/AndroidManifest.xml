<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practicas">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_Transferencias"></activity>
        <activity android:name=".Activity_medios_de_pago" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity_mapas"
            android:label="@string/title_activity_mapas" />
        <activity android:name=".Activity_LoadinDialog" />
        <activity android:name=".Activity_Login" />
        <activity android:name=".Huella" />
        <activity android:name=".Beneficiarios" />
        <activity android:name=".DiscreteScrollViewOptions" />
        <activity android:name=".TarjetasAdapter" />
        <activity android:name=".Activity_tarjetas" />
        <activity android:name=".Item" />
        <activity android:name=".Activity_Pago" />
        <activity android:name=".drawer_layout" />
        <activity android:name=".Inicio" />
        <activity
            android:name=".Activity_Splach_Screem"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>